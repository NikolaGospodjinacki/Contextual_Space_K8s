apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: contextual-space
    component: backend
spec:
  selector:
    app: contextual-space
    component: backend
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  type: ClusterIP
