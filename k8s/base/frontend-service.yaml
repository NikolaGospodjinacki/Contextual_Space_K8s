apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: contextual-space
    component: frontend
spec:
  selector:
    app: contextual-space
    component: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
